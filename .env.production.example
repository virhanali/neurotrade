# NeuroTrade Production Environment Configuration
# IMPORTANT: Copy this file to .env and update all values before deployment
# DO NOT commit .env file to version control (it's in .gitignore)

################################################################################
# Database Configuration
################################################################################
POSTGRES_USER=neurotrade
POSTGRES_PASSWORD=CHANGE_THIS_STRONG_PASSWORD_123!@#
POSTGRES_DB=neurotrade_db

################################################################################
# Security Configuration
################################################################################
# CRITICAL: Generate a strong random secret for production
# Generate with: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_KEY_FOR_PRODUCTION_USE_OPENSSL

################################################################################
# AI Engine Configuration
################################################################################
# Get from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your_deepseek_api_key_here

# Get from: https://aistudio.google.com/app/apikey
OPENROUTER_API_KEY=OPENROUTER_API_KEY

################################################################################
# Telegram Notifications
################################################################################
# Get bot token from: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Get your chat ID from: https://t.me/userinfobot
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

################################################################################
# Trading Configuration (PRODUCTION VALUES)
################################################################################
# CRITICAL: Set back to 70-85 for production (10 is for testing only!)
MIN_CONFIDENCE=75.0

# Set to false in production to reduce logs
DEBUG_MODE=false

# Scan interval in minutes (30 = every 30 minutes)
SCAN_INTERVAL=30

# Default paper trading balance (USDT)
DEFAULT_BALANCE=1000.0

################################################################################
# System Configuration
################################################################################
# Timezone for notifications and logs
# Options: Asia/Jakarta, America/New_York, Europe/London, etc.
TZ=Asia/Jakarta

# Redis configuration (usually default is fine)
REDIS_HOST=redis
REDIS_PORT=6379

# Python engine URL (internal Docker network)
PYTHON_ENGINE_URL=http://python-engine:8000

################################################################################
# Production Checklist
################################################################################
# [ ] Changed POSTGRES_PASSWORD to strong password
# [ ] Generated new JWT_SECRET (use: openssl rand -base64 32)
# [ ] Added real API keys for DeepSeek and Gemini
# [ ] Configured Telegram bot token and chat ID
# [ ] Set MIN_CONFIDENCE back to 75.0 (or higher)
# [ ] Set DEBUG_MODE=false
# [ ] Verified timezone is correct
# [ ] Reviewed firewall rules (UFW)
# [ ] SSL certificate obtained (or basic auth enabled)
# [ ] Backup strategy in place
